var TableManageDefault = function () {
    "use strict";

    var runDataTable = function () {
        var table = $('#data-table');

        // ✅ Cegah inisialisasi ulang
        if (!$.fn.DataTable.isDataTable(table)) {
            table.DataTable({
                responsive: true
                // konfigurasi tambahan lainnya jika perlu
            });
        }
    };

    return {
        // public function
        init: function () {
            runDataTable();
        }
    };
}();